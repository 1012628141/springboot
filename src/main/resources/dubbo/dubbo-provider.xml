<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://code.alibabatech.com/schema/dubbo
    http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

   <!-- &lt;!&ndash; 提供方应用信息，用于计算依赖关系 &ndash;&gt;
    <dubbo:application name="${dubbo.application.name}" />

    &lt;!&ndash; 注册中心暴露服务地址 &ndash;&gt;
    <dubbo:registry protocol="${dubbo.registry.protocol}" address="${dubbo.registry.address}" />
    &lt;!&ndash;  暴露服务&ndash;&gt;
    <dubbo:protocol name="${dubbo.protocol.name}" port="${dubbo.protocol.port}" />

    <dubbo:service interface="com.zjy.springboot.inteface.DubboInteface" ref="dubboService" retries="0" timeout="6000" />

    <bean id="dubboService" class="com.zjy.springboot.service.imp.DubboIntefaceImpl"/>-->
</beans>